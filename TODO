o Add a way for the names to be changed by the owner (perhaps a block,
  like 'memcached_accessor(:foo, :bar) { |*args| "name" }' ?)
  Otherwise there's no way to share these 'instance variables' between
  objects..
